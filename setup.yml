---
- name: Setup Docker-Server
  hosts: localhost
  remote_user: root

  tasks:

  - name: Hello Server
    debug:
      msg: "Hello Steffi!" 
      
  - name: Create an insecure file
    file:
      path: /home/hv11134/test.txt
      state: touch
